#include <iostream>
#include <string>

int* string_split(std::string a);

int main(){
    std::string input = "";
    std::getline(std::cin, input);
    
    
}

int* string_split(char* a)
{
    char* nums_char[2];
    int* nums[2];
    strtok(a, " ");
}